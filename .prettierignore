# Build output and caches
dist
.output
.nuxt
.next
.astro
node_modules
coverage
pnpm-lock.yaml

# The example apps are wiring demos, each following its own framework's
# conventions. Reformatting them to this repo's style would say nothing useful.
examples

# Generated by changelogen.
CHANGELOG.md

# Markdown is prose here: prettier reflows paragraphs and renumbers lists, which
# makes documentation diffs unreadable for no gain.
*.md

# YAML holds the workflow comments explaining why each guard exists; leave their
# wrapping alone.
*.yml
*.yaml
